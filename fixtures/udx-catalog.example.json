{
  "$comment": "SHAPE EXAMPLE ONLY — not a live @udx/lib dump. Do not copy these as real UDX APIs.",
  "designSystem": "udx",
  "source": { "package": "@udx/lib", "version": "0.0.82" },
  "components": [
    {
      "selector": "udx-button",
      "status": "verified",
      "name": "Button",
      "react": "Button",
      "inputs": {
        "variant": ["primary", "secondary", "ghost"],
        "size": ["sm", "md", "lg"]
      },
      "tokens": ["color.action.primary", "space.200", "radius.md"]
    }
  ]
}
